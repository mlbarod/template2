# 사용방법

이 프로젝트는 `make` 명령으로 Docker Compose를 실행합니다.
일반 사용자는 긴 `docker compose -f ...` 명령을 직접 입력하지 않습니다.

## 먼저 고르는 것

실행할 때는 환경을 먼저 고릅니다.

| 환경 | 명령 | 용도 | dependency source |
| --- | --- | --- | --- |
| `dev` | `make dev` | 로컬 개발 전용 | public registry/package source |
| `oidc` | `make oidc` | 사내 OIDC 검증/스테이징 | internal mirror |
| `prod` | `make prod` | 운영 compose 조립 | internal mirror |

기억할 규칙은 하나입니다.

```text
dev = local + public
oidc/prod = internal + mirror
```

`dev`는 로컬 PC에서만 쓰는 개발 환경입니다.
`repository.samsungds.net` 같은 내부 mirror 주소를 사용하지 않습니다.

`oidc`와 `prod`는 내부망 기준 환경입니다.
Docker image, npm, pip, apt, Alpine package source를 내부 mirror로 고정합니다.

## app과 infra

서비스는 `app`과 `infra`로 나눕니다.

| 그룹 | 포함 서비스 | 설명 |
| --- | --- | --- |
| `app` | API, Web, Nginx, MinIO, MinIO init | 실제 앱 실행에 필요한 서비스 |
| `dev app` 추가 | dummy ADFS/RAG/LLM/Mail/Jira | 로컬 외부계 대체 서비스 |
| `infra` | Airflow DB, Airflow init/webserver/scheduler, FTP | 데이터 적재와 Airflow DAG 검증용 기반 서비스 |

일반 로컬 개발은 `app`만 실행합니다.
Airflow/FTP가 필요한 데이터 적재 작업을 할 때만 `infra`를 별도로 실행합니다.

## 로컬 개발

대부분의 개발은 이 명령으로 시작합니다.

```bash
make dev
```

`make dev`는 내부적으로 `dev-app-up`을 실행합니다.
API가 의존하는 DB는 compose 의존성으로 함께 준비되고, Airflow/FTP는 실행하지 않습니다.

로컬 개발 중 자주 쓰는 명령:

```bash
make dev-app-up
make dev-app-build
make dev-app-down
```

데이터 적재나 Airflow DAG 확인이 필요할 때만 infra를 사용합니다.

```bash
make dev-infra-up
make dev-infra-build
make dev-infra-down
```

## OIDC 검증

사내 OIDC/실제 연동 검증은 아래 명령을 사용합니다.

```bash
make oidc
```

`make oidc`는 infra를 먼저 올리고 app을 올립니다.
이 환경은 내부 mirror를 사용합니다.
OIDC infra에는 Airflow/FTP 외에 monitoring 서비스도 포함됩니다.

필요하면 app과 infra를 따로 조작할 수 있습니다.

```bash
make oidc-app-up
make oidc-app-build
make oidc-app-down

make oidc-infra-up
make oidc-infra-build
make oidc-infra-down
```

## 운영 구성

운영 compose 조립은 아래 명령을 사용합니다.

```bash
make prod
```

`make prod`도 내부 mirror를 사용합니다.
`oidc`와 `prod`의 차이는 dependency source가 아니라 env 값, 도메인, secret, 운영 설정입니다.

필요하면 app과 infra를 따로 조작할 수 있습니다.

```bash
make prod-app-up
make prod-app-build
make prod-app-down

make prod-infra-up
make prod-infra-build
make prod-infra-down
```

OIDC/prod compose에는 Airflow/FTP 외에 monitoring 서비스도 포함됩니다.

## 전체 종료

세 compose project를 모두 내립니다.

```bash
make down
```

특정 환경의 일부 서비스만 내릴 때는 `*-app-down`, `*-infra-down`을 사용합니다.

## 검증 명령

백엔드 검증은 개발 API 컨테이너 기준으로 실행합니다.

```bash
make check-api
make test-api
make makemigrations-check
```

프론트엔드 검증은 npm workspace 명령을 사용합니다.

```bash
npm run web:lint
npm run web:build
```

## 파일 구조

root에는 사람이 실행 기준으로 보는 compose 파일과 Makefile만 둡니다.

```text
Makefile
docker-compose.dev.yml
docker-compose.oidc.yml
docker-compose.yml
```

내부 조립용 compose 파일은 `compose/` 아래에 있습니다.

```text
compose/airflow.yml
compose/airflow.internal.yml
compose/dev.app.yml
compose/dev.infra.yml
compose/oidc.app.yml
compose/oidc.infra.yml
compose/prod.app.yml
compose/prod.infra.yml
compose/monitoring.yml
```

`compose/` 파일은 일반 사용자가 직접 실행하지 않습니다.
문제가 생겼을 때 config 확인이나 디버깅 목적으로만 직접 봅니다.

## mirror 참고

환경별 dependency source 정책은 `docs/configuration.md`에 있습니다.
전체 proxy mirror 목록은 `docs/integrations/proxy-mirrors.md`에 있습니다.
